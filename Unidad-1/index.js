let nombre = "Facundo";

let id = 2;

//Boolean
let administrador = false;

//Null
let usuario = null;

//Undefined
let data;

let fecha = new Date().toLocaleString();

function authUser() {};

class User {};

const estudiantes = ["Constanza", "Carlos", "Franco"];

const estudiante = {
  nombre : "Constanza",
  curso: "Backend 1"
}

//console.log( estudiante.curso );

function sumar(num1, num2) {
  return num1 + num2;
}

sumar(10, 20);

const sumarArrowFunction = (num1, num2) => num1 + num2;

sumarArrowFunction(10, 20);